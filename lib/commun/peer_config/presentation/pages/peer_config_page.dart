import 'package:flutter/material.dart';

class PeerConfigPage extends StatelessWidget {
  const PeerConfigPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PeerConfig'),
      ),
    );
  }
}
